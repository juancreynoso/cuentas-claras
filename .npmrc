# npm 10.9 crashea resolviendo los peers opcionales de vitest 4
# (@vitest/browser, webdriverio, @vitest/ui) con el error
# "Cannot read properties of null (reading 'edgesOut')".
# `npm ci` funciona sin este flag; sólo lo necesita el install inicial.
legacy-peer-deps=true
